{% extends 'base.html.twig' %}

{% block title %}Edit Camion{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <!-- Sidebar chargée depuis base.html.twig -->
        <div class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">Modifier le Camion</h1>
            </div>

            <div class="card shadow mb-4">
                <div class="card-body">
                    {{ include('camion_crud/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>

            <div class="mt-3">
                <a href="{{ path('app_camion_c_r_u_d_index') }}" class="btn btn-secondary me-2">
                    Retour à la liste
                </a>
                {{ include('camion_crud/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}